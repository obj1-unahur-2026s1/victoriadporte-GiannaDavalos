import example.pepita

describe "group of tests for pepita" {

  test "pepita has initial energy" {
    assert.equals(100, pepita.energy())
  }

test "victoria se parece a otro deportista" {
  assert.equals(150, victoria.altura())
}
}